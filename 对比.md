# 三种 Git 协作方式对比

三种方式都可以使用。主要区别是：**什么时候创建 bb 的 PR，以及在哪里解决冲突。** 三种流程都以本地 master 同步完成结束。

## 主要区别

| 对比项 | 第一种：先解决冲突，再创建 PR | 第二种：先创建 PR，在本地解决冲突 | 第三种：先创建 PR，在 GitHub 解决冲突 |
| --- | --- | --- | --- |
| 对应文档 | [先解决再提 PR](git-team-workflow.md) | [先提 PR，本地解决](git-team-workflow-pr-first.md) | [先提 PR，网页解决](git-team-workflow-github-conflicts.md) |
| bb 何时创建 PR | aa 合并后，bb 先解决冲突，再创建 PR | aa、bb 都先创建 PR，再合并 aa | aa、bb 都先创建 PR，再合并 aa |
| 冲突在哪里解决 | 本地 bb | 本地 bb | GitHub 的 PR 页面 |
| 解决后先更新谁 | 本地 bb | 本地 bb | 远程 bb |
| 解决后如何继续 | push bb，再创建 PR | push bb，原 PR 自动更新 | 点击 Commit merge，原 PR 自动更新 |
| 是否需要新建 bb 的 PR | 此时首次创建 | 不需要，继续使用原 PR | 不需要，继续使用原 PR |
| 主分支何时接收 bb 的结果 | bb 的 PR 合并时 | bb 的 PR 合并时 | bb 的 PR 合并时 |
| 如何验证合并结果 | 可以在本地运行代码、测试后再创建 PR | 可以在本地运行代码、测试后再更新 PR | 依靠 CI，或拉回本地验证 |
| 最后一步 | 拉取远程 master，同步本地 master | 拉取远程 master，同步本地 master | 拉取远程 master，同步本地 master |
| 适用情况 | 想先整理和测试完整，再交给同事审查 | 需要尽早讨论、审查，并持续修改代码 | 简单、容易判断的文本冲突 |

## 三种操作顺序

**第一种：先在本地准备好，再提交 PR。**

```text
aa 的 PR 合并
→ bb 获取最新主分支并合入
→ 在本地解决冲突、检查和测试
→ push bb
→ 创建并合并 bb 的 PR
→ 同步本地 master
```

**第二种：先提交 PR，在本地继续修改。**

```text
创建 aa、bb 两个 PR
→ 合并 aa 的 PR
→ bb 的 PR 与更新后的主分支有冲突
→ 在本地 bb 获取并合入主分支，解决冲突、检查和测试
→ push bb，原 PR 自动更新
→ 合并 bb 原来的 PR
→ 同步本地 master
```

**第三种：先提交 PR，直接在 GitHub 网页处理冲突。**

```text
创建 aa、bb 两个 PR
→ 合并 aa 的 PR
→ bb 的 PR 与更新后的主分支有冲突
→ 在 GitHub 点击 Resolve conflicts，编辑冲突文件
→ 点击 Mark as resolved，再点击 Commit merge，更新远程 bb
→ 完成必要检查，合并 bb 原来的 PR
→ 同步本地 master
```

第三种的 `Commit merge` 是把 master 合入远程 bb；之后的 `Merge pull request` 才是把 bb 合入远程 master。PR 合并后，只为获取最终主分支代码，不需要先拉取 bb，可以直接同步本地 master。[GitHub 冲突处理说明](https://docs.github.com/en/pull-requests/how-tos/merge-and-close-pull-requests/resolving-a-merge-conflict-on-github)

## 团队开发如何选择

**建议将第二种作为日常默认流程。** 提前创建 PR，可以让同事了解改动、开始讨论；后续修改和冲突处理都继续提交到同一个 PR。代码尚未准备好时，可以先创建 Draft PR，准备完成后再请求正式审查。[GitHub PR 说明](https://docs.github.com/en/pull-requests/reference/pull-requests)

- **第一种**：适合任务较小，希望先完成整合和测试再交审。提前解决冲突后，主分支仍可能继续变化，后续也可能需要再次同步。
- **第二种**：适合多人协作、持续审查，以及涉及多个文件或功能逻辑的修改。
- **第三种**：适合 README、注释等容易判断的简单文本冲突。GitHub 网页编辑器主要支持简单的文本行冲突，复杂情况应使用本地工具。[GitHub 冲突处理说明](https://docs.github.com/en/pull-requests/how-tos/merge-and-close-pull-requests/resolving-a-merge-conflict-on-github)

这是一种工作方式建议，不是必须遵守的固定顺序。没有冲突且满足团队合并要求时，可以直接合并 PR，不必专门执行一次冲突处理。

## 结合 AI 协作开发如何选择

**如果你主要在本地使用 AI 修改代码，同样推荐第二种。** 你可以让 AI 查看双方改动、协助解决冲突，并在本地检查和运行测试，再推送到已有 PR。

```text
你和 AI 在功能分支开发、检查和测试
→ push，创建 PR
→ 根据审查意见继续修改，必要时同步主分支、解决冲突
→ 检查合并结果，重新运行相关测试
→ push，更新原 PR
→ 审查和检查通过，合并 PR
→ 同步本地 master
```

AI 去掉了冲突标记，并不代表合并后的功能正确。应检查双方需要的逻辑是否保留，并验证相关功能；团队可以通过分支保护要求审查批准和自动检查通过后再合并。[GitHub 分支保护说明](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)

**日常默认选第二种；简单文本冲突可选第三种；想准备完整后再交审时选第一种。**
